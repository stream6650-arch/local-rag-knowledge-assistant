# Retrieval-Augmented Generation

RAG retrieves relevant external knowledge before generating an answer. Typical stages include ingestion, chunking, vectorization or embedding, retrieval, optional reranking, prompt construction and generation. Poor retrieval can still lead to weak answers.